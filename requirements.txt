# Core pipeline. TensorRT itself is installed inside the Colab notebook so it
# matches the runtime's CUDA build (the pip wheel is environment-specific).
ultralytics>=8.3.0
imageio[ffmpeg]>=2.34.0
pillow>=10.0.0
pyyaml>=6.0
numpy>=1.24
